# Platform Merge Inventory

Dokumen ini mencatat penggabungan histori dari `platform.klubfisika.or.id` ke repo `community.klubfisika.or.id/apps/platform`.

## Hasil merge

- Merge commit: `51aeabd`
- Histori platform yang diikutkan:
  - `f09ce58` — fix badge logos
  - `6e889b0` — rewrite README dengan badges, architecture, dan feature table
  - `58032b2` — rewrite README dengan platform vision, features, dan gamification
- Histori community tetap berada di jalur utama `main`.

## Catatan penting

- Merge ini memakai strategi non-destruktif agar tidak ada histori yang hilang.
- Tree code utama tetap mengikuti repo community, karena struktur Astro/Qwik tidak bisa dicampur secara literal tanpa konflik besar.
- Snapshot dokumen legacy platform juga sudah diarsipkan di `docs/legacy/platform-repo/`.
- Untuk aset dokumentasi dan referensi historis, gunakan file ini bersama README repo dan tag arsip yang sudah ada.
