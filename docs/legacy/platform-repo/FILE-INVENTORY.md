# Platform Repo File Inventory

This folder preserves the textual artifacts from the legacy `platform.klubfisika.or.id` repository so the repo can be archived or deleted without losing its documentation trail.

Preserved files:

- `README.md`
- `TESTING_CONVENTION.md`

Note:
- The full code history is already represented in the community repo through the non-destructive merge branch and commit history.
- This folder exists as a human-readable snapshot of the legacy repo's most important documentation.
