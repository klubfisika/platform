# Legacy Platform Restore Archive Note

This directory preserves the textual documentation from `klubfisika.github.io` so the source repo can be archived or deleted without losing historical context.
